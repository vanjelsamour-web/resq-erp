import React,{useEffect,useState} from 'react';import {Edit3,Plus,Power,X} from 'lucide-react';
type User={id:string;username:string;name:string;email:string;roles:string[];active:boolean};const roles=[['DOCTOR','Doctor'],['NURSE','Nurse'],['MANAGER','Manager'],['ACCOUNTING','Accounting']];
export default function Users(){const[data,setData]=useState<User[]>([]);const[show,setShow]=useState(false);const[editing,setEditing]=useState<User|null>(null);const[error,setError]=useState('');const[form,setForm]=useState({name:'',username:'',email:'',password:'',roles:['DOCTOR']});const load=async()=>{const r=await fetch('/api/users',{credentials:'include'});const x=await r.json();if(r.ok)setData(x);else setError(x.error||'Unable to load users')};useEffect(()=>{load()},[]);const openNew=()=>{setEditing(null);setForm({name:'',username:'',email:'',password:'',roles:['DOCTOR']});setShow(true)};const openEdit=(u:User)=>{setEditing(u);setForm({name:u.name,username:u.username,email:u.email||'',password:'',roles:u.roles?.length?u.roles:['DOCTOR']});setShow(true)};const toggle=(r:string)=>setForm(x=>({...x,roles:x.roles.includes(r)?x.roles.filter(v=>v!==r):[...x.roles,r]}));const save=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(!form.roles.length)return setError('Select at least one role');if(!editing&&!form.password)return setError('Password is required');const r=await fetch(editing?`/api/users/${editing.id}`:'/api/users',{method:editing?'PATCH':'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const x=await r.json();if(!r.ok){setError(x.error||'Unable to save user');return}setShow(false);await load()};const toggleActive=async(u:User)=>{const r=await fetch(`/api/users/${u.id}`,{method:'PATCH',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({active:!u.active,roles:u.roles,name:u.name,email:u.email})});const x=await r.json();if(!r.ok)setError(x.error||'Unable to update user');else load()};return <><div className="page-heading"><div><p className="eyebrow">ACCESS CONTROL</p><h1>Users</h1><p className="muted">Login accounts for RESQ ERP. Users can have multiple roles.</p></div><button className="primary" onClick={openNew}><Plus size={17}/> New user</button></div>{error&&<div className="error-banner" style={{marginBottom:16}}>{error}</div>}<section className="panel"><div className="table-wrap"><table><thead><tr><th>Name</th><th>Username</th><th>Email</th><th>Role(s)</th><th>Status</th><th></th></tr></thead><tbody>{data.map(u=><tr key={u.id}><td><strong>{u.name}</strong></td><td>{u.username}</td><td>{u.email||'—'}</td><td>{u.roles.map(r=><b className="badge review" style={{marginRight:6}} key={r}>{roles.find(x=>x[0]===r)?.[1]||r}</b>)}</td><td><b className={`badge ${u.active?'approved':'rejected'}`}>{u.active?'Active':'Inactive'}</b></td><td><button className="link-button" onClick={()=>openEdit(u)}><Edit3 size={14}/> Edit</button><button className="link-button" onClick={()=>toggleActive(u)}><Power size={14}/> {u.active?'Deactivate':'Activate'}</button></td></tr>)}{!data.length&&<tr><td colSpan={6} className="no-results">No users found.</td></tr>}</tbody></table></div></section>{show&&<div className="modal-backdrop" onMouseDown={()=>setShow(false)}><div className="modal" onMouseDown={e=>e.stopPropagation()}><div className="modal-header"><div><p className="eyebrow">USER ACCOUNT</p><h2>{editing?'Edit user':'New user'}</h2></div><button className="modal-close" onClick={()=>setShow(false)}><X size={20}/></button></div><div className="form-grid"><label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Username<input required disabled={Boolean(editing)} value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password{editing&&<small className="cell-sub">Leave blank to keep current password.</small>}<input type="password" minLength={8} required={!editing} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label></div><label style={{display:'block',marginTop:16}}>Roles<div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10,marginTop:8}}>{roles.map(([value,text])=><label key={value} style={{display:'flex',alignItems:'center',gap:8}}><input type="checkbox" checked={form.roles.includes(value)} onChange={()=>toggle(value)}/>{text}</label>)}</div></label><div className="form-actions"><button className="secondary" onClick={()=>setShow(false)}>Cancel</button><button className="primary" onClick={save}>{editing?'Save changes':'Create user'}</button></div></div></div>}</>}
