import React,{useEffect,useState} from 'react';import {BarChart3,Boxes,FileText,Receipt,Users,Wallet} from 'lucide-react';
export default function Reports(){const[s,setS]=useState<any>(null);const[c,setC]=useState<any[]>([]);const load=async()=>{const[a,b]=await Promise.all([fetch('/api/reports/summary'),fetch('/api/reports/cases')]);if(a.ok)setS(await a.json());if(b.ok)setC(await b.json())};useEffect(()=>{load()},[]);if(!s)return <section className="empty-module"><div><div className="empty-icon"><BarChart3/></div><h2>Reports</h2><p>Loading live reports from PostgreSQL...</p></div></section>;const cards=[['Financial summary','Revenue, VAT, expenses and profit',Wallet],['Invoices & payments','Collections and outstanding balances',Receipt],['Case profitability','Revenue, direct costs and margin per case',FileText],['Inventory','Stock value and low-stock items',Boxes],['Staff activity','Warehouse withdrawals and audit trail',Users],['VAT','Output VAT, input VAT and net payable',BarChart3]];return <><div className="page-heading"><div><p className="eyebrow">MANAGEMENT REPORTING</p><h1>Reports</h1><p className="muted">Live operational and financial reporting.</p></div></div><div className="report-grid">{cards.map(([title,text,Icon])=><div className="report-card" key={title}><div className="report-icon"><Icon size={24}/></div><strong>{title}</strong><span>{text}</span></div>)}</div><div className="stats-grid" style={{marginTop:20}}><div className="stat-card"><span>Net revenue</span><strong>€{s.revenue.toFixed(2)}</strong></div><div className="stat-card"><span>Expenses</span><strong>€{s.expenses.toFixed(2)}</strong></div><div className="stat-card"><span>Operating profit</span><strong>€{s.grossProfit.toFixed(2)}</strong><small>{s.profitMargin.toFixed(1)}% margin</small></div><div className="stat-card"><span>VAT payable</span><strong>€{s.vatPayable.toFixed(2)}</strong><small>Output €{s.outputVat.toFixed(2)} − Input €{s.inputVat.toFixed(2)}</small></div></div><section className="panel"><div className="panel-header"><div><h2>Profitability by case</h2><p>Net revenue less materials and expenses.</p></div></div><div className="table-wrap"><table><thead><tr><th>Case</th><th>Patient</th><th>Revenue</th><th>Cost</th><th>Profit</th><th>Margin</th></tr></thead><tbody>{c.map(x=><tr key={x.id}><td>{x.id}</td><td>{x.patient}</td><td>€{x.revenue.toFixed(2)}</td><td>€{x.cost.toFixed(2)}</td><td>€{x.profit.toFixed(2)}</td><td>{x.margin.toFixed(1)}%</td></tr>)}</tbody></table></div></section></>}
