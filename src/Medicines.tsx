import React,{useEffect,useState} from 'react';import {Plus,X} from 'lucide-react';
type Medicine={id:string;name:string;strength:string;form:string;status:string};export default function Medicines(){const[data,setData]=useState<Medicine[]>([]);const[show,setShow]=useState(false);const[form,setForm]=useState({name:'',strength:'',form:''});const load=async()=>{const r=await fetch('/api/medicines');if(r.ok)setData(await r.json())};useEffect(()=>{load()},[]);const create=async(e:React.FormEvent)=>{e.preventDefault();const r=await fetch('/api/medicines',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(r.ok){setShow(false);setForm({name:'',strength:'',form:''});load()}};return <><div className="page-heading"><div><p className="eyebrow">MEDICINES</p><h1>Medicines</h1><p className="muted">Medicine catalogue and patient prescribing base.</p></div><button className="primary" onClick={()=>setShow(true)}><Plus size={17}/> New medicine</button></div><section className="panel"><div className="table-wrap"><table><thead><tr><th>Medicine</th><th>Strength</th><th>Form</th><th>Status</th></tr></thead><tbody>{data.map(m=><tr key={m.id}><td><strong>{m.name}</strong></td><td>{m.strength||'—'}</td><td>{m.form||'—'}</td><td><b className="badge approved">{m.status}</b></td></tr>)}{!data.length&&<tr><td colSpan={4} className="no-results">No medicines yet.</td></tr>}</tbody></table></div></section>{show&&<div className="modal-backdrop" onMouseDown={()=>setShow(false)}><div className="modal" onMouseDown={e=>e.stopPropagation()}><div className="modal-header"><div><p className="eyebrow">MEDICINE CATALOGUE</p><h2>New medicine</h2></div><button className="modal-close" onClick={()=>setShow(false)}><X size={20}/></button></div><form onSubmit={create}><div className="form-grid"><label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Strength<input value={form.strength} onChange={e=>setForm({...form,strength:e.target.value})}/></label><label>Form<input value={form.form} onChange={e=>setForm({...form,form:e.target.value})} placeholder="Tablet, syrup..."/></label></div><div className="form-actions"><button type="button" className="secondary" onClick={()=>setShow(false)}>Cancel</button><button className="primary">Create medicine</button></div></form></div></div>}</>}
