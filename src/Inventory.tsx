import React,{useEffect,useState} from 'react';
import {ArrowDownToLine,ArrowUpFromLine,Plus,Search,X} from 'lucide-react';
type Item={id:string;name:string;category:string;unit:string;stock:number;minStock:number;unitCost:number;billablePrice:number;expiryDate:string;batchNo:string};
type Employee={id:string;name:string;role:string};
export default function Inventory(){const[items,setItems]=useState<Item[]>([]);const[employees,setEmployees]=useState<Employee[]>([]);const[movements,setMovements]=useState<any[]>([]);const[search,setSearch]=useState('');const[show,setShow]=useState<'issue'|'receipt'|'item'|null>(null);const[selected,setSelected]=useState<Item|null>(null);const[error,setError]=useState('');const[form,setForm]=useState<any>({quantity:'',employeeId:'',patientId:'',caseId:'',reason:'',unitCost:'',code:'',name:'',category:'Medical material',unit:'pcs',stock:'0',minStock:'0',billablePrice:'0'});
 const load=async()=>{try{const[r,e,m]=await Promise.all([fetch('/api/inventory'),fetch('/api/employees'),fetch('/api/inventory/movements')]);if(!r.ok)throw new Error('Unable to load inventory');setItems(await r.json());setEmployees(e.ok?await e.json():[]);setMovements(m.ok?await m.json():[])}catch(e){setError(e instanceof Error?e.message:'Unable to load inventory')}};useEffect(()=>{load()},[]);
 const filtered=items.filter(i=>`${i.id} ${i.name} ${i.category}`.toLowerCase().includes(search.toLowerCase()));
 const issue=async(e:React.FormEvent)=>{e.preventDefault();if(!selected)return;const r=await fetch(`/api/inventory/${encodeURIComponent(selected.id)}/issue`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok){setError(d.error);return}setShow(null);await load()};
 const receipt=async(e:React.FormEvent)=>{e.preventDefault();if(!selected)return;const r=await fetch(`/api/inventory/${encodeURIComponent(selected.id)}/receipt`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({quantity:form.quantity,unitCost:form.unitCost,reason:form.reason})});const d=await r.json();if(!r.ok){setError(d.error);return}setShow(null);await load()};
 const create=async(e:React.FormEvent)=>{e.preventDefault();const r=await fetch('/api/inventory',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok){setError(d.error);return}setShow(null);await load()};
 return <><div className="page-heading"><div><p className="eyebrow">WAREHOUSE</p><h1>Inventory</h1><p className="muted">Stock, receipts and traceable withdrawals by employee, patient and case.</p></div><button className="primary" onClick={()=>setShow('item')}><Plus size={17}/> New item</button></div><div className="stats-grid"><div className="stat-card"><span>Items</span><strong>{items.length}</strong></div><div className="stat-card"><span>Low stock</span><strong>{items.filter(i=>i.stock<=i.minStock).length}</strong></div><div className="stat-card"><span>Inventory value</span><strong>€{items.reduce((s,i)=>s+i.stock*i.unitCost,0).toFixed(2)}</strong></div><div className="stat-card"><span>Movements</span><strong>{movements.length}</strong></div></div><div className="patient-toolbar"><div className="patient-search"><Search size={18}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search item or category..."/></div></div><section className="panel"><div className="table-wrap"><table><thead><tr><th>Code</th><th>Item</th><th>Category</th><th>Stock</th><th>Cost</th><th>Charge</th><th>Status</th><th>Action</th></tr></thead><tbody>{filtered.map(i=><tr key={i.id}><td>{i.id}</td><td><strong>{i.name}</strong><small className="cell-sub">{i.unit}</small></td><td>{i.category}</td><td>{i.stock}</td><td>€{i.unitCost.toFixed(2)}</td><td>€{i.billablePrice.toFixed(2)}</td><td><b className={`badge ${i.stock<=i.minStock?'pending':'approved'}`}>{i.stock<=i.minStock?'Low stock':'In stock'}</b></td><td><button className="link-button" onClick={()=>{setSelected(i);setForm({quantity:'',employeeId:'',patientId:'',caseId:'',reason:'',unitCost:String(i.unitCost)});setShow('issue')}}><ArrowUpFromLine size={14}/> Issue</button> <button className="link-button" onClick={()=>{setSelected(i);setForm({quantity:'',unitCost:String(i.unitCost),reason:''});setShow('receipt')}}><ArrowDownToLine size={14}/> Receive</button></td></tr>)}</tbody></table></div></section><section className="panel" style={{marginTop:20}}><div className="panel-header"><div><h2>Recent stock movements</h2><p>Who took what, for which patient and case.</p></div></div><div className="table-wrap"><table><thead><tr><th>Date</th><th>Item</th><th>Movement</th><th>Employee</th><th>Patient</th><th>Case</th><th>Qty</th><th>Charge</th></tr></thead><tbody>{movements.slice(0,20).map(m=><tr key={m.id}><td>{new Date(m.date).toLocaleString()}</td><td>{m.itemName}</td><td>{m.type}</td><td>{m.employeeName}</td><td>{m.patientId??'—'}</td><td>{m.caseId??'—'}</td><td>{m.quantity}</td><td>€{m.chargedAmount.toFixed(2)}</td></tr>)}</tbody></table></div></section>
 {show&&<div className="modal-backdrop" onMouseDown={()=>setShow(null)}><div className="modal" onMouseDown={e=>e.stopPropagation()}><div className="modal-header"><div><p className="eyebrow">WAREHOUSE</p><h2>{show==='issue'?'Issue stock':show==='receipt'?'Receive stock':'New inventory item'}</h2></div><button className="modal-close" onClick={()=>setShow(null)}><X size={20}/></button></div><form onSubmit={show==='issue'?issue:show==='receipt'?receipt:create}><div className="form-grid">{show==='item'?<><label>Code<input required value={form.code} onChange={e=>setForm({...form,code:e.target.value})}/></label><label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Category<input required value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/></label><label>Unit<input value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})}/></label><label>Opening stock<input type="number" step="0.001" value={form.stock} onChange={e=>setForm({...form,stock:e.target.value})}/></label><label>Minimum stock<input type="number" step="0.001" value={form.minStock} onChange={e=>setForm({...form,minStock:e.target.value})}/></label><label>Unit cost<input type="number" step="0.01" value={form.unitCost} onChange={e=>setForm({...form,unitCost:e.target.value})}/></label><label>Patient charge<input type="number" step="0.01" value={form.billablePrice} onChange={e=>setForm({...form,billablePrice:e.target.value})}/></label></>:show==='issue'?<><label>Employee<select required value={form.employeeId} onChange={e=>setForm({...form,employeeId:e.target.value})}><option value="">Select employee...</option>{employees.map(e=><option key={e.id} value={e.id}>{e.id} — {e.name} ({e.role})</option>)}</select></label><label>Quantity<input required type="number" min="0.001" step="0.001" value={form.quantity} onChange={e=>setForm({...form,quantity:e.target.value})}/></label><label>Patient ID<input value={form.patientId} onChange={e=>setForm({...form,patientId:e.target.value})} placeholder="RSQ-P-000001"/></label><label>Case ID<input value={form.caseId} onChange={e=>setForm({...form,caseId:e.target.value})} placeholder="RSQ-C-000001"/></label><label>Reason<input value={form.reason} onChange={e=>setForm({...form,reason:e.target.value})}/></label></>:<><label>Quantity<input required type="number" min="0.001" step="0.001" value={form.quantity} onChange={e=>setForm({...form,quantity:e.target.value})}/></label><label>Unit cost<input required type="number" min="0" step="0.01" value={form.unitCost} onChange={e=>setForm({...form,unitCost:e.target.value})}/></label><label>Reason<input value={form.reason} onChange={e=>setForm({...form,reason:e.target.value})}/></label></>}</div><div className="form-actions"><button type="button" className="secondary" onClick={()=>setShow(null)}>Cancel</button><button className="primary">Save</button></div></form></div></div>}</>}
